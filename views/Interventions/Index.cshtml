@model IEnumerable<DataLayer.Entities.PatientEntities.Intervention>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Clinic.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Intervention_start_day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intervention_end_day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intervention_type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Intervention_start_day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intervention_end_day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intervention_type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Intervention_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Intervention_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Intervention_id })
        </td>
    </tr>
}

</table>
